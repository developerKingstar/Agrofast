<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F4F6F4"
    android:padding="16dp"
    tools:context=".Show_order">

    <!-- 1. Horizontal Header Bar - now 4 tabs: Received, Orders, Completed, Declined -->
    <LinearLayout
        android:id="@+id/tabHeaderLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:orientation="horizontal"
        android:weightSum="4">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnReceivingOffers"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="46dp"
            android:layout_marginEnd="4dp"
            android:layout_weight="1"
            android:paddingHorizontal="0dp"
            android:text="Received"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            app:cornerRadius="10dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPendingOrders"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="46dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:layout_weight="1"
            android:paddingHorizontal="0dp"
            android:text="Orders"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            app:cornerRadius="10dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCompletedOrders"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="46dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:layout_weight="1"
            android:paddingHorizontal="0dp"
            android:text="Completed"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            app:cornerRadius="10dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDeclinedOrders"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="46dp"
            android:layout_marginStart="4dp"
            android:layout_weight="1"
            android:paddingHorizontal="0dp"
            android:text="Declined"
            android:textColor="#FFFFFF"
            android:textSize="10sp"
            android:textStyle="bold"
            app:cornerRadius="10dp" />

    </LinearLayout>

    <!-- 2. Search Bar -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/searchOrderInputLayout"
        style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tabHeaderLayout"
        android:layout_marginTop="16dp"
        android:hint="Search orders or buyers..."
        app:boxStrokeColor="#2E7D32"
        app:hintTextColor="#2E7D32"
        app:startIconDrawable="@android:drawable/ic_menu_search"
        app:startIconTint="#8D6E63">
        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/etSearchOrders"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="text"
            android:textColor="#1B4D3E"
            android:textSize="14sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- 3. Dynamic Fragment Container -->
    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/searchOrderInputLayout"
        android:layout_marginTop="16dp" />

</RelativeLayout>
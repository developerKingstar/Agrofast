<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item>
        <shape android:shape="rectangle">
            <!-- Replace hardcoded solid color with a theme attribute -->
            <solid android:color="?attr/CardBackgroundColor" />

            <!-- Replace hardcoded stroke color with a theme attribute -->
            <stroke
                android:width="1dp"
                android:color="?attr/CardStrokeColor" />

            <corners android:radius="16dp" />
            <padding
                android:left="8dp"
                android:right="8dp"
                android:top="8dp"
                android:bottom="8dp" />
        </shape>
    </item>
</ripple>